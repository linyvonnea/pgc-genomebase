// src/components/sample-form/SampleFormPDF.tsx
// Canonical re-export — keeps this folder the developer's entry-point while
// the actual implementation lives in components/pdf/ (used by API routes).
export { SampleSubmissionFormPDF as SampleFormPDF } from "@/components/pdf/SampleSubmissionFormPDF";
